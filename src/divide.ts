/**
 * Exposed function example for number division
 * @param first number that located before "÷" symbol
 * @param second number that located after "÷" symbol
 * @returns division result
 */
export function divide(first: number, second: number): number {
	return first / second;
}

export default divide;
