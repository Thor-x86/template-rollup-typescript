/**
 * Exposed function example for number subtraction
 * @param first number that located before "-" symbol
 * @param second number that located after "-" symbol
 * @returns subtraction result
 */
export function subtract(first: number, second: number): number {
	return first - second;
}

export default subtract;
