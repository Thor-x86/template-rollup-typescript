/**
 * Exposed function example for number multiplication
 * @param first number that located before "x" symbol
 * @param second number that located after "x" symbol
 * @returns multiplication result
 */
export function multiply(first: number, second: number): number {
	return first * second;
}

export default multiply;
